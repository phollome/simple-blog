{
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build": "npx tsc",
    "start": "node build/index.js"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "typescript": "^4.9.4",
    "vitest": "^0.26.2"
  },
  "dependencies": {
    "del": "^7.0.0",
    "fs-extra": "^11.1.0"
  }
}
